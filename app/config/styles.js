import colors from "./colors";

export default styles = {
    input: {
        borderWidth: 1,
        borderColor: colors.black,
        padding: 10,
        fontSize: 18,
        borderRadius: 6
    }
}